{
  "name": "cypress",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm run build",
    "start": "cypress run",
    "cy:open": "cypress open",
    "cy:run": "cypress run --project ./cypress/e2e/1-getting-started --browser chrome --spec '**/*.cy.js'",
    "cy:run:wait": "wait-on https://example.cypress.io/todo && npm run cy:run"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@applitools/eyes-cypress": "^3.23.9",
    "cypress": "^13.13.0"
  },
  "devDependencies": {
    "mocha": "^10.6.0"
  }
}
